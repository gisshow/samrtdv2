import React, { Component } from 'react'
import DataPanel from '@/components/dataPanel'
import { connect } from 'dva'
class RoomStat extends Component {

  render (){
    return (
      <>
        {/* {roomStatistic && <DataPanel data={roomStatistic} type="Column" isExpand={false} padding={[0,0,30,0]}/>}
        {populationStatistic && <DataPanel data={populationStatistic} type="Bar" isExpand={false} padding={[0,20,40,100]}/>} */}
      </>
    );
  }
}

export default RoomStat